#include <stdio.h>
#include "header.h"
#include 

#define test;
#define
#define test

typdef struct a{
    int i
    int a
};

typedef struct b{
    int i
    int a
};

typedef struct c {
    int i;
    int a;
} test;


enum week{Mon, Tue, Wed, Thur, Fri, Sat, Sun}; //correto
enum week{Mon, Tue, Wed, Thur, Fri, Sat, Sun}

int main(void) {
    int i;
    char str[] = {"foo"};
    char a = 'i'

    for (i = 0; i < 10; i++) printf("bar\n");
    for (i = 0 < i < 10; i++) {
        printf("test\n");
    }

    while(i--) printf("foo\n");
    while(i < 10) {
        i++;
    }

    switch(a){
        case a == '0':
            printf("foo\n");
            break;
        case a == '1':
            printf("bar\n");
            break;
        default:
            printf("Não lembro mais como usar isso\n");
    }

    i = 5;
    do{
        printf("foo\n")
    }while(i--);
 
}